"use strict";

const buttonsLogic = {
    init() {
        //add an event listener to the 'changeColorToRed' button that calls changeColor
        

        //add an event listener to the 'addButton' that calls addButton
    },
    addButton() {
        //add a button to the html

        //add an event listener to this button that calls changeColor, use the input field as parameter
    },
    changeColor(color){
        //change the background-color of body according to the param
        
    }
};

buttonsLogic.init();